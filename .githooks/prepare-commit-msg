#!/bin/sh
# Strip Cursor co-author trailers from every commit message.
sed -i.bak '/^Co-authored-by: Cursor/d' "$1" 2>/dev/null || \
  sed -i '' '/^Co-authored-by: Cursor/d' "$1" 2>/dev/null
rm -f "$1.bak"
